# processes/

Only real movements. Index by filename.

| Process | One line |
|---------|----------|
| `create-instance.md` | POST /instances + cupo + async Green job |
| `account-status.md` | POST /account/status snapshot |
| `activate-plan-cupo.md` | Platform activate writes max_instances + rotates token |
